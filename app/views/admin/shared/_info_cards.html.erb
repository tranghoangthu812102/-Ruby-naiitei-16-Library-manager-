<div class="col-4">
  <div class="card">
    <div class="card-header">
      <%= title %>
    </div>
    <div class="card-body">
      <h5 class="card-title"><%= title %></h5>
      <p class="card-text">
        <%= t ".quantity" %><%= model.count %>
      </p>
      <%= link_to "Manage", path, class: "btn btn-primary" %>
    </div>
  </div>
</div>
