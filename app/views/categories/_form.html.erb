<%= form_for @category do |f| %>
  <%= render "shared/errors", object: f.object %>

  <%= f.label t ".name" %>
  <%= f.text_field :name, class: "form-control" %>

  <%= f.submit yield(:button_text), class: "btn btn-primary form" %>
<% end %>
